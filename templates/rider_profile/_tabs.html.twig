{% import "macros/macros.html.twig" as macros %}
<div class="details-tabs" role="tablist" aria-label="{{ 'general.nav.rider_profile'|trans }}">
	{{ macros.tab(
        'fa-solid fa-medal',
        'rider.galop_history',
        'tab-galops',
        true
    ) }}

	{{ macros.tab(
        'fa-solid fa-trophy',
        'rider.competitions',
        'tab-competitions'
    ) }}

	{{ macros.tab(
        'fa-solid fa-horse',
        'rider.horses',
        'tab-horses'
    ) }}
</div>
